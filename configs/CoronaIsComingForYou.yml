# test data file with filepaths
test_data: "colorme/val_Tiny_ImageNet_no_grayscale.csv"

# seed to use for random state
random_seed: 725

# Batch size during testing
batch_size: 32

# Eval Mode during testing (Should -probably- be True)
eval_mode: True

# number of workers for dataloader
num_workers: 4
